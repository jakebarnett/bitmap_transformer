This program will augment the color palette of a .bmp file.  It takes the name of the original file as the first argument, the name of the new file as the second argument, and a color transformation as a third.

example: node index.js myBitmap.bmp myNewBitmap.bmp randomize

Color options: random, red, blue, green .
